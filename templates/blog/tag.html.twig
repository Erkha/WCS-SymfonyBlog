{% extends 'base.html.twig' %}

{% block title %}{{ tag.name}}{% endblock %}

{% block body %}
    <h1>All articles from {{tag.name}} : </h1>
    {% for article in articles %}
        <div>
            <a href="{{ path('blog_show',{slug :article.title}) }}"><h2>{{ article.id }} / {{ article.title }}</h2></a>
            <p>{{ article.content }}</p>
        </div>
    {% else %}
        Aucun article trouvé.
    {% endfor %}
		<a href="{{ path('app_index') }}">
			Back to homepage.
		</a>
		
{% endblock %}